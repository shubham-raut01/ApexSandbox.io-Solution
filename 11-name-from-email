public String nameFromEmail(String email) {
String[] parts = email.toLowerCase().split('@')[0].split('\\.');
        String firstName = parts[0].capitalize();
        String lastName = parts[1].capitalize();
        
        return firstName + ' ' + lastName;
}